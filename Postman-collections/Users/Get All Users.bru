meta {
  name: Get All Users
  type: http
  seq: 4
}

get {
  url: {{normalUrl}}/users
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}

docs {
  Get all users (no authentication required for this endpoint).
}
