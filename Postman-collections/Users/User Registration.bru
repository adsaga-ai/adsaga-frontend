meta {
  name: User Registration
  type: http
  seq: 1
}

post {
  url: {{normalUrl}}/users/register
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "fullname": "Keval Maru",
    "email": "kevalmaru5623@gmail.com",
    "password": "kiyaxoxo"
  }
}

settings {
  encodeUrl: true
}

docs {
  Register a new user. Organisation ID is optional and can be provided later when creating an organisation.
}
