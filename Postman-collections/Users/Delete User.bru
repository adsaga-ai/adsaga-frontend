meta {
  name: Delete User
  type: http
  seq: 8
}

delete {
  url: {{normalUrl}}/users/{{userId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}

docs {
  Delete user by user ID.
}
